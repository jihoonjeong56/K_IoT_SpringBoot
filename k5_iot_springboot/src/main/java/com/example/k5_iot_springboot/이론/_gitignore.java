package com.example.k5_iot_springboot.이론;
// 이미 올라간 파일 제거하는 방법

// 1) Git 추적에서만 제거 ( 파일은 로컬에 유지

//  : --cached 옵션(깃에서만 제거, 로컬에서는 유지)

// git rm --cached application-secret.properties

// 2) commit & push

// git commit -m "remove application-secret.properties from repository"
// git push origin main
public class _gitignore {
}
